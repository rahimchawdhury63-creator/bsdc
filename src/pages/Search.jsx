/**
 * src/pages/Search.jsx
 * Wrapper page so the router has a clean entry.
 */
import React from 'react';
import SearchResults from '../components/search/SearchResults.jsx';

export default function Search() {
  return <SearchResults />;
}
