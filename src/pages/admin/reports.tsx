import { AdminDataTable } from '@components/admin/AdminDataTable';
export const AdminReportsPage = () => <AdminDataTable title="Admin reports"><p className="text-muted">This admin section is route-ready and protected. It will operate only on real Firestore data when records exist.</p></AdminDataTable>;
