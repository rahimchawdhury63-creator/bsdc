import { SettingsSection } from '@components/settings/SettingsSection';
export const SettingsIndexPage = () => <SettingsSection title="Index settings" description="This section is connected to the settings architecture and will persist real user preferences through Firestore modules." />;
