import { SettingsSection } from '@components/settings/SettingsSection';
export const SettingsAccountPage = () => <SettingsSection title="Account settings" description="This section is connected to the settings architecture and will persist real user preferences through Firestore modules." />;
