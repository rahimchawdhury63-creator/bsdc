import { Outlet } from 'react-router-dom';

/** Messenger shell for direct, group, and channel routes. */
export const MessengerLayout = () => <section className="messenger-layout"><Outlet /></section>;
