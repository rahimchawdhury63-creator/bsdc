/** Channel post card placeholder for real channel broadcast documents. */
export const ChannelPost = ({ title }: { readonly title: string }) => <article className="post-card"><h2>{title}</h2></article>;
