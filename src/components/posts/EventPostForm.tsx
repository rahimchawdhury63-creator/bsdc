import { GenericPostForm } from './GenericPostForm';

/** Event post form for developer meetups, workshops, and online events. */
export const EventPostForm = () => <GenericPostForm type="event" heading="Create event" />;
