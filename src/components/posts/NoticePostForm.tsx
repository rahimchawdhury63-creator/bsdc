import { GenericPostForm } from './GenericPostForm';

/** Notice form for announcements and admin broadcasts. */
export const NoticePostForm = () => <GenericPostForm type="notice" heading="Create notice" />;
