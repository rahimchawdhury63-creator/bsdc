import { GenericPostForm } from './GenericPostForm';

/** Text post form for short updates with tags, visibility, and optional images. */
export const TextPostForm = () => <GenericPostForm type="text" heading="Create text post" />;
