import { GenericPostForm } from './GenericPostForm';

/** Resource sharing form for links, tools, and learning materials. */
export const ResourcePostForm = () => <GenericPostForm type="resource" heading="Share resource" />;
