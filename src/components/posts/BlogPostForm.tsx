import { GenericPostForm } from './GenericPostForm';

/** Long-form blog post form using the rich text editor shell. */
export const BlogPostForm = () => <GenericPostForm type="blog" heading="Write blog post" richText />;
