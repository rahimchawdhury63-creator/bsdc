import { SVGIcon } from '@components/ui/SVGIcon';

/** SVG-only verification badge displayed for verified BSDC profiles. */
export const VerifiedBadge = () => <span className="verified-badge"><SVGIcon name="shield" width={16} height={16} decorative /> Verified</span>;
