/**
 * Client-visible sitemap helper placeholder for Response 8.
 * Real sitemap.xml generation will be handled by Firebase Cloud Functions so
 * crawlers receive XML without depending on browser JavaScript.
 */
export const SitemapGenerator = () => null;
